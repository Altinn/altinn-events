name: Label critical PR
on:
  pull_request:
    branches:
      -  main
    types:
      - opened
    paths:
      - '**/Migration/**'

jobs:
  add_labels:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/labeler@v4
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"