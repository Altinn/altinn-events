info:
  name: App published event (nin)
  type: http
  seq: 5

http:
  method: POST
  url: "{{PlatformHostUrl}}/events/api/v1/outbound"
  headers:
    - name: Content-Type
      value: application/cloudevents+json
    - name: PlatformAccessToken
      value: "{{AccessToken}}"
  body:
    type: json
    data: |-
      {
        "specversion": "1.0",
        "id": "{{$randomUUID}}",
        "time": "{{$isoTimestamp}}",
        "type": "app.instance.created",
        "source": "https://ttd.apps.at23.altinn.cloud/ttd/apps-test/instances/50660768/428a4575-2c04-4400-89a3-1aaadd2579cd",
        "resource": "urn:altinn:resource:app_ttd_apps-test",
        "resourceinstance": "50660768/428a4575-2c04-4400-89a3-1aaadd2579cd",
        "subject": "/party/50660768",
        "alternativesubject": "/person/07837399275"
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: await bru.runRequest("Token generator/access token (platform_events)");

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
