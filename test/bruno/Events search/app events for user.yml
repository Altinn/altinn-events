info:
  name: app events for user
  type: http
  seq: 2

http:
  method: GET
  url: "{{PlatformHostUrl}}/events/api/v1/app/party?from=2026-06-20T11:00:53.3917Z&party={{AuthN_PartyId}}"
  params:
    - name: from
      value: 2026-06-20T11:00:53.3917Z
      type: query
    - name: party
      value: "{{AuthN_PartyId}}"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: await bru.runRequest("Token generator/subscribe (user)");

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
