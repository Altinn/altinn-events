meta {
  name: app subscription (person)
  type: http
  seq: 4
}

post {
  url: {{PlatformHostUrl}}/events/api/v1/subscriptions
  body: json
  auth: inherit
}

body:json {
  {
    "sourceFilter": "https://ttd.apps.at23.altinn.cloud/ttd/apps-test",
    "endpoint": "https://webhook.site/a637c7c2-4c9c-419b-8ea5-49c8ae84126e",
    "subjectFilter": "/party/{{AuthN_PartyId}}"
  }
}

script:pre-request {
  await bru.runRequest("Token generator/subscribe (user)");
}

settings {
  encodeUrl: true
  timeout: 0
}
