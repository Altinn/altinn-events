meta {
  name: publish (ttd)
  type: http
  seq: 3
}

get {
  url: {{TokenGeneratorBaseUrl}}/api/GetEnterpriseToken?env={{Environment}}&scopes=altinn:events.publish&orgNo=991825827&org=ttd
  body: none
  auth: basic
}

params:query {
  env: {{Environment}}
  scopes: altinn:events.publish
  orgNo: 991825827
  org: ttd
}

auth:basic {
  username: {{TokenGeneratorUserName}}
  password: {{TokenGeneratorPassword}}
}

script:post-response {
  const token = res.getBody();
  bru.setVar("BearerToken", token);
}

settings {
  encodeUrl: true
  timeout: 0
}
