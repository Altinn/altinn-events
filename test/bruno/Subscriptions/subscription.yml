info:
  name: subscription
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{PlatformHostUrl}}/events/api/v1/subscriptions/96165"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
