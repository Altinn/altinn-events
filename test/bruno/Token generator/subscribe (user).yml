info:
  name: subscribe (user)
  type: http
  seq: 2

http:
  method: GET
  url: "{{TokenGeneratorBaseUrl}}/api/GetPersonalToken?env={{Environment}}&scopes=altinn:events.subscribe&userId={{AuthN_UserId}}&partyId={{AuthN_PartyId}}&pid={{AuthN_Pid}}"
  params:
    - name: env
      value: "{{Environment}}"
      type: query
    - name: scopes
      value: altinn:events.subscribe
      type: query
    - name: userId
      value: "{{AuthN_UserId}}"
      type: query
    - name: partyId
      value: "{{AuthN_PartyId}}"
      type: query
    - name: pid
      value: "{{AuthN_Pid}}"
      type: query
  auth:
    type: basic
    username: "{{TokenGeneratorUserName}}"
    password: "{{TokenGeneratorPassword}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const token = res.getBody();
        bru.setVar("BearerToken", token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
